import { z } from 'zod';
import { profileSchema } from '@/components/profile-setup/types';

// Export the schema from types.ts to keep it consistent across the application
export const profileSetupSchema = profileSchema;
